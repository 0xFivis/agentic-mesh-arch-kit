<!-- REFERENCE ONLY: sanitized sample, not for production -->
# ops/slo/

> Service Level Objectives，机器可读 YAML。每个服务 1 个 `<service-name>.yaml`，引用 `docs/architecture/_registry.yaml#services.<name>.slo`。
